{
  "name": "rubix-mania",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "restore-source": "node scripts/restore-source.mjs",
    "predev": "npm run restore-source",
    "dev": "vite",
    "prebuild": "npm run restore-source",
    "build": "tsc && vite build",
    "pretypecheck": "npm run restore-source",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
